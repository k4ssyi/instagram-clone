    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to "Instagram Clone", root_path, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li><form><input type="text" onfocus="placeholders()" value="キーワードを入力" id="placeholder" /></form></li>
              <li><%= link_to "通知",   top_notice_path %></li>
              <li><%= link_to "プロフィール", user_path(current_user.id) %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
